OTA updates

HW WDT
test wifi autoconnect reconnect
    turn on device when there are not any network
    disable network, check reconnect to another network 
    disable all network, check reconnect
Diagnostic of free memory free Heap, ESP.getFreeHeap().
